500. Keyboard Row

Example:

Input: ["Hello", "Alaska", "Dad", "Peace"]
Output: ["Alaska", "Dad"]

var findWords = function(words) {
  //create obj with letters set to the values of their row example qwer=1 asdf=2 zxcv=3
  //create matchedWords variable set to empty [];

  //iterate over the words input array
    //create lowerWord to words index set to toLowerCase()
    //create sum variable

    //iterate over lowerWord
      //add each value to sum

      // if index is equal to last index && sum === length of word * the value at the key
        //push that word onto the matchedWords array

  //return the matchedWord array
};